## How to use Docker

***
Download Docker Desktop from Internet

  Windows Users
    (https://docs.docker.com/desktop/setup/install/windows-install/)

  Mac Users
    (https://docs.docker.com/desktop/setup/install/mac-install/)

and start the Docker Desktop

Now open your code using vscode

in root of the code you can identified "_docker-compose.yml_" file

## Getting Started


## 🛠️ Running with Docker


Now Check For Backend:
**[http://localhost:5001](http://localhost:5001)**

Now Open Frontend:

**[http://localhost:5173](http://localhost:5173)**

---

## 🧪 Using Docker Compose

```bash
docker-compose up -d
```

### if Any Package  adding to the package.json file then need to follow the steps



### Stop containers

```bash
docker-compose down
```

## 🧼 Cleaning Up

Remove unused containers/images:

```bash
docker system prune -a
```

```bash
docker-compose up -d
```

